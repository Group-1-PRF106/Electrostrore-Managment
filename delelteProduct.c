#include "declareFunction.h"
#include "data.h"
#include <stdio.h>

void deleteProduct(void) {
	loadData();
	printf("deleteProduct: function not implemented yet.\n");
	// Placeholder: real implementation should open NextFile.dat, copy all
	// records except the one to delete into a temp file, then replace file.
}